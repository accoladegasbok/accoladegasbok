deployment:
  tasks:
    - export DEPLOYPATH=/home/autodlrn/public_html
    - /bin/rsync -rltD --delete --exclude='.env' --exclude='vendor' --exclude='/storage' --exclude='.git' ./ $DEPLOYPATH
    - chmod 755 $DEPLOYPATH